{% extends "main.html" %}
{% block main %}
    {% load staticfiles %}
    <!-- Page Heading -->

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Detail Produk <!--<small>Statistics Overview</small>-->
            </h1>
            <ol class="breadcrumb">
                <li class="active">
                    <i class="fa fa-home"></i> <a href="{% url 'home' %}">Home</a>
                </li>
                <li>
                    <i class="fa fa-user"></i> Menu
                </li>
                <li>
                    <i class="fa fa-user"></i> Detail Produk
                </li>
            </ol>
        </div>
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-6">





    harga
    gambar

            <h1>{{ object.kode_produk }}</h1>

            <p>{{ object.nama_produk }}</p>

            <p>Reporter: {{ object.deskripsi }}</p>

            <p>Published: {{ object.keterangan }}</p>

        </div>
    </div>
{% endblock %}
